---
title: "C++中的智能指针"
layout: post
author: "Marco"
header-style: text
hidden: true
tags:
  - c++
  - 指针
---

## C++中的智能指针

### 1. 基本概念

- ***为什么需要指针？***

  ​	指针是内存地址的抽象，计算机的操作本来就是基于内存地址的，其他语言只是出于降低编程难度，隐藏了指针，而隐藏了指针的后果就是性能的额外开销。而指针使用其中一个难点就在于，如何确定指针以及所指对象的声明周期，尤其是多个指针指向同一个对象。例如，为了在多个集合中拥有同一个对象，必须要给每个集合传入指针。在理想的情况下，其中一个指针被销毁了，没有任何问题会发生（即没有空悬指针，也不会重复删除被引用的对象），当指向对象的最后一个指针被销毁时，该对象才会被销毁（即没有资源泄漏）。

  ​	而为了避免不限于上述带来的指针问题，智能指针是常用的解决方案。

- ***什么是智能指针？***

  ​	智能指针就是在普通指针之上加了一层封装机制，这样的一层封装机制的目的是为了使得智能指针可以方便地管理一个对象的声明周期。

- ***有哪些智能指针？***

  ​	auto_ptr、unique_ptr、shared_ptr和weak_ptr。 模板auto_ptr是C++98提供的解决方案，C+11已将将其摒弃，并提供了另外两种解决方案。然而，虽然auto_ptr被摒弃，但它已使用了好多年：同时，如果您的编译器不支持其他两种解决力案，auto_ptr将是唯一的选择。

- ***四种智能指针的区别***

  > [参考1](https://www.iamshuaidi.com/2419.html)


### 2. 实战篇

- ***智能指针的实现***

  [这里](https://github.com/spratt/SmartPointer)

